为什么要使用模块化？
  解决命名冲突
  提供复用性
  提高代码可维护性

都有哪几种方式可以实现模块化？各有什么特点？
  自执行函数
  CommonJS
    支持动态导入
    同步导入
    在导出时都是值拷贝就算导出的值变了导入的值也不会改变、所以如果想更新值必须重新导入一次
  ES Module
    不支持动态导入
    异步导入
    导入导出的值都指向同一个内存地址
    会编译成 require/exports 来执行
