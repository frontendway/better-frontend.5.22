http版本
  0.9
    只有 get 命令
    没有 header 等描述数据的信息
    服务器发送完毕就关闭了 tcp 连接
  1.0
    增加了很多命令、status code 和 header
    多字符集支持、多部分发送、权限、缓存等
  1.1
    支持持久连接
      Connection: Keep-Alive
      1.0 版本中 1 个 http 请求就会在客户端服务端之间创立 1 个 tcp连接
      服务端返回内容后 tcp 连接就会关闭
    pipeline
      同一个 tcp 连接中可发多个请求但是有先后顺序的、服务端处理也是按照顺序返回、所以还是会导致出现并发的 tcp 请求
    增加 host 和其他命令
    每次请求与响应很多头信息必须完整的发送与返回、比如 content-type cache-control 都是以字符串保存、占用带宽的量较大
  2
    所有数据二进制传输、1.1 版本大部分是通过字符串传输
    头信息压缩以及推送等提高效率的功能
    同一个 tcp 连接中可以并发多个 http 请求、服务端无须按顺序处理返回、整个网站可以只需要一个 tcp 连接
    推送
      服务端可主动发送数据传输
      请求 html 同时把此 html 需要的 css js 文件推送给客户端、html css js 的发送顺序是并行
    chrome 浏览器支持的 tcp 连接并发数量是 6 个
  3
    因为 HTTP/2 使用了多路复用，一般来说同一域名下只需要使用一个 TCP 连接。当这个连接中出现了丢包的情况，那就会导致 HTTP/2 的表现情况反倒不如 HTTP/1 了。
    因为在出现丢包的情况下，整个 TCP 都要开始等待重传，也就导致了后面的所有数据都被阻塞了。
    基于 UDP 协议的 QUIC 协议

数据协商
  请求
    Accept
      指定希望接受的数据类型
    Accept-Encoding
      希望返回的数据以怎样的压缩格式进行压缩
    Accept-Language
    User-Agent
    Content-Type
      application/x-www-form-urlencoded
        数据以字符串进行传输、只是查询字符串数据放到 body 中发送数据
        encodeURIComponent(k) = encodeURIComponent(v)
      multipart/form-data
        本次请求有多个部分、当上传文件时必须把文件单独拆出来因为文件是不能以字符串、必须以二进制数据传输
        以 Content-Type 中的 boundary 的值进行分割发送的数据
      application/json
      application/javascript
  响应
    Content-Type     对应 Accept
    Content-Encoding 对应 Accept-Encoding
    Content-Language 对应 Accept-Language

http状态码
  定义服务器对请求的处理结果
  2xx
    成功
    204 请求处理成功、但无资源可返回
    206 表示客户端进行了范围请求、此 get 请求由响应报文中的 Content-Range 指定范围的实体内容
  3xx
    重定向
    301 永久重定向、服务端重定向一次后就直接访问重定向后的地址、会尽可能长时间缓存 301 的资源
      配合 location 响应头
    302 临时重定向、每次都会去服务端重定向路径

    303 由于请求对应的资源存在着另一个 URI、应使用 GET 方法定向获取请求的资源
    304 不经常更新的资源服务器会结合客户端设置 304 缓存、再次加载从缓存中获取、如果资源更新了服务器会根据最后修改时间来强制让客户端从服务器拉取最新数据
  4xx
    客户端错误
    400 表示请求报文中存在语法错误
    401 表示发送的请求需要有通过 HTTP 的认证信息、另外若之前已进行过 1 次请求则表示用户认证失败
    403 表明对请求资源的访问被服务器拒绝了、服务端无须给出拒绝理由、如若需要说明则可在主体部分进行描述
    405 客户端请求的方法虽然能被服务器识别、但是服务器禁止使用该方法
  5xx
    服务端错误
    502 表明扮演网关或代理角色的服务器、从上游服务器中接收到的响应是无效的
    503 表明服务器暂时处于超负载或正在进行停机维护、现在无法处理请求

URI
  统一资源标志符
  用来唯一标识互联网上的信息资源、包含 URL、URN
URL
  统一资源定位符
  由 协议+域名+端口号+路径+查询字符串 组成
URN
  永久统一资源定位符、资源移动后还能被找到
